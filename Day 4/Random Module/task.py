import random

# random_integer = random.randint(1,10)
# print(random_integer)
# The random.random() functions are not used with bounds.
rand_numb = random.random()
#  The random.uniform() functions care used with bounds.
random_float = random.uniform(1,10)
